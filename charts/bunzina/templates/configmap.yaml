apiVersion: v1
kind: ConfigMap
metadata:
  name: bunzina-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "bunzina.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}