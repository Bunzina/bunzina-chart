apiVersion: v1
kind: Service
metadata:
  name: bunzina
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "bunzina.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bunzina.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
