{{- if .Values.database.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.database.name }}-secret
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "app-chart.labels" (dict "name" .Values.database.name "root" $) | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.database.secret }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
